// import { from, interval, Observable, of, timer } from 'rxjs';
// import '../../assets/css/style.css';
// import { terminalLog } from '../../utils/log-in-terminal';
// import { ajax } from 'rxjs/ajax';

// // interval(1000).subscribe(terminalLog);

// // timer(5000, 1000).subscribe(terminalLog);

// // of(1, 2, [3, 4], {count: 5}).subscribe(terminalLog);
// // of(1, 2, [3, 4], {count: 5}).subscribe(console.log);

// // from([1, 2, [3, 4], {count: 5}]).subscribe(console.log);
// // from({length: 5}).subscribe(console.log);
// from(
//     fetch('')
// ).subscribe(console.log);

// ajax({
//     url: '/',
//     crossDomain: true,
//     method: 'GET',
// }).subscribe(console.log)
